[Unit]
Description=kubelet

[Service]
ExecStart=/usr/bin/kubelet --address=0.0.0.0 --api-servers=http://localhost:8080 --allow-privileged=true --network-plugin=cni --node-labels=$node_labels
Restart=always

[Install]
WantedBy=multi-user.target
